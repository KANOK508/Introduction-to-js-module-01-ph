const pen = { brand: "econo", price: 10, color: "black" };
const pencil = new Object();  // what is the work on ---new Object ? 
console.log(pencil);
const rubber = Object.create({});
console.log(rubber);

// using new

/* 
Each key value -pair called -----------Property */
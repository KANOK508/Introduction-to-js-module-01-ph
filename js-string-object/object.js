// primitive
// basic, primary
const age = 21;
const school = 'rifle square';
const isPassed = true;
let isDeveloper;  // only define --that is now undefined......
console.log(isDeveloper)

const subjects = ['bangla', 'English', 'Physics', 'Math']
const bottle_wrong = ['white', 45];  // that is not meaningful
const bottleColor = 'white';
const bottlePrice = 45;
const bottleDrinks = 'water';

// object-- (For store all the information of a Particular object we use the JS Object system.....)
// non-primitive
const bottle = {
    brand: 'apple',
    price: 45,
    color: 'white',
    isClean: false,
}

// non-primitive
// here , subject is OBJECT name, ---name, teacher ,examDate  properties or attributes like or keys----with values..
const subject = {
    name: 'biology',
    teacher: 'rasheda mam',
    examDate: '30 Feb', 
    chapters: ['first', 'second', 'third'],
    exams: {
        name: 'Final Exam',
        marks: 100
    }
}

/*  For Declare an array we ---use ---    Third bracket ,   [ Comma separated values....]
   for Declare  an Object we ---use ---      Second Bracket, {  key:value pairs} 

object/ className /Entity /Anything__what_we_see_as-Thing        -- Ex --  bottle, subject, 

    property/ attributes/ key         -- brand, price , color
    
    

*/

/* 
✅ Corrected & polished version (recommended)

To declare an array, we use square brackets [ ] with comma-separated values.

To declare an object, we use curly brackets { } with key : value pairs.

About this part 👇

object / className / Entity — Ex — bottle, subject
property / attributes / key — brand, price, color

✅ Concept is correct, just rewrite it like this:

Object / Class / Entity represents a real-world thing
Example: bottle, subject

Property / Attribute / Key represents details of the object
Example: brand, price, color*/
